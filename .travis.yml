
langage: c
os: linux
dist: bionic
cache: false

compiler:
  - gcc
  - clang

install:
 - sudo apt install zlib1g-dev

before_script:
  - autoreconf -vif

script:
  - ./configure --enable-wall --enable-dwarfexample && make && make check
