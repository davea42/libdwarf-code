
langage: c
os: linux
dist: bionic
cache: false

compiler:
  - gcc
  - clang

install:
 - sudo apt install zlib1g-dev

before_script:
  - ./autogen.sh

script:
  - ./configure --enable-wall --enable-dwarfexample && make && make check
