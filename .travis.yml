
langage: c
os: linux
dist: bionic
cache: false

compiler:
  - gcc
  - clang

install:
  - sudo apt-get -qq update
  - sudo apt install build-essential zlib1g-dev mailutils mutt

before_script:
  - autoreconf -vif

script: 
  - ./configure --enable-wall --enable-dwarfexample && make && make check
